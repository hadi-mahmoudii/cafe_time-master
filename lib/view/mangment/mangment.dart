import 'package:flutter/material.dart';

class MangmentPage extends StatelessWidget {
  const MangmentPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}